each topic in topics
  a(href=topic.url).topic-container
    .topic-card
      - if (topic.coverUrl)
        .pic(style='background-image:url(' + topic.coverUrl + ')')
      - else
        .no-pic
      .title=topic.mediaTitle